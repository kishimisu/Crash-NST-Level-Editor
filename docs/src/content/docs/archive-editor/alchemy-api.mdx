---
title: Alchemy API
description: Use the Alchemy namespace from C# to open archives, find and edit objects in IGZ files, and save the result.
sidebar:
  order: 5
---

import { FileTree } from '@astrojs/starlight/components';

The editor is built on a custom C# library, the `Alchemy` namespace, that you can use directly to script archive edits. It needs the [.NET SDK 9.0](https://dotnet.microsoft.com/en-us/download/dotnet/9.0) and the source code of the project.

## Example

Open a level archive, move every entity of the crates file up by 100 units, and save:

```csharp
// Open archive
IgArchive archive = IgArchive.Open(archiveDir + "L101_NSanityBeach.pak");

// Find archive file
IgArchiveFile file = archive.FindFile("L101_NSanityBeach_Crates.igz", FileSearchType.NameWithExtension)!;

// Convert to igz file
IgzFile igz = file.ToIgzFile();

// List objects of type CEntity
List<CEntity> entities = igz.FindObjects<CEntity>();

// Move all entities up
entities.ForEach(entity => entity._parentSpacePosition._z += 100.0f);

// Save igz file in archive
file.SetData(igz.Save());

// Save updated archive to disk
archive.Save();
```

## Project structure

<FileTree>

- src/
  - Alchemy/ Alchemy-related classes
    - IgArchive/ `.pak` files
    - IgzFile/ `.igz` files
    - igObject/ Alchemy objects
  - Havok/ Havok-related classes
    - HavokFile/ `.hkx` files
    - hkObject/ Havok objects
  - Editor/ the editor application
  - Utils/ common utilities
  - Program.cs entry point

</FileTree>

The source code is on [GitHub](https://github.com/kishimisu/Crash-NST-Level-Editor). Questions about the internals are welcome in the `#nst-level-editor` or `#alchemy` channels of the [Alchemy Reverse Engineering](https://discord.gg/vsnVrPvVjc) discord server.
